import ProfileTab from "@/components/profile/Profile";
export const metadata = {
  title: {
    absolute: "Perfil",
  },
};

const page = () => {
    return (
      <>
        <ProfileTab />
      </>
    )

};

export default page;
